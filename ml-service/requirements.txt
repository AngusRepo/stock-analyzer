fastapi==0.139.0
starlette==1.3.1
uvicorn==0.30.6
numpy==1.26.4
polars>=1.0.0
# training data processing remains Polars + NumPy.
pandas[pyarrow]>=2.2.0
finlab==2.0.13
# Tree models, hmmlearn, and SHAP share the scikit-learn runtime.
scikit-learn==1.9.0
# L1.25 strategy similarity evidence runs in Modal/Python, not Worker TS.
# NetworkX owns graph components; sklearn-extra owns official KMedoids/PAM.
networkx==3.6.1
scikit-learn-extra==0.3.0
xgboost==3.2.0
statsmodels==0.14.4
httpx==0.27.2
pydantic==2.9.2
joblib==1.4.2
google-cloud-storage==2.18.2
aiohttp==3.14.1
Pillow>=12.3.0
Mako==1.3.12
zipp>=3.19.1
hmmlearn==0.3.2
arch==7.0.0
lightgbm==4.6.0
# PyTorch is required by DLinear/PatchTST/GNN/TabM/TimesFM runtime paths.
# 2.13 removes the former setuptools<82 resolver conflict and supports the
# restricted weights-only deserialization policy enforced by this service.
torch==2.13.0
# Security floors for neuralforecast transitive runtime dependencies.
ray==2.56.0
# 2.6.1 is the official safe release for CVE-2026-31221 and predates the
# compromised 2.6.2/2.6.3 releases. NeuralForecast's stale <2.6 metadata is
# overridden by installing its reviewed wheel with --no-deps after this base.
pytorch-lightning==2.6.1
coreforecast==0.0.17
fsspec[http]==2026.4.0
lightning-utilities==0.15.3
PyYAML==6.0.3
torchmetrics==1.9.0
tornado==6.5.7
utilsforecast==0.2.16
cryptography==48.0.1
idna==3.15
msgpack==1.2.1
setuptools==83.0.0
# Breeze2 reason-generation shadow provider. Transformers 5.13 retains the
# AutoModel/AutoTokenizer/GenerationConfig APIs used by the MediaTek helper and
# removes the vulnerable 4.47 deserialization stack.
transformers==5.13.1
accelerate>=0.26.0
mtkresearch==0.3.2
safetensors>=0.4.5
timm>=1.0.22
einops==0.8.2
# River：ARF 聚合層（第11模型 Layer 2），ADWIN drift detection
river>=0.21.0
# P3#31: SHAP feature attribution for tree models
shap>=0.46.0
# P3#29: RL Shadow Framework
gymnasium>=0.29.0
stable-baselines3>=2.3.0
# P3#28: GNN / GraphSAGE production artifact runtime.
torch-geometric==2.8.0
# Maintained-library sequence production adapters.
# P7 research benchmarks: not production voters; used by research_model_benchmark
# to produce OOS IC / CPCV-PBO / cost / data-slice evidence.
tabm==0.0.3
# TimesFM 2.5 runtime package. Serving still loads config-backed artifacts and
# fails closed if the active GCS config points at an incompatible archived model.
timesfm[torch]==2.0.1
# 2026-04-07 Phase 1: Optuna routes (auto-search + push KV)
optuna==4.9.0
# Monthly strategy mining uses pymoo NSGA-III + novelty in Modal.
pymoo==0.6.1.6
# 2.0 Feature Selection: Target Permutation elbow detection
kneed>=0.8.0
