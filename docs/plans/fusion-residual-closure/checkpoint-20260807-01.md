# Checkpoint 01: v14 serving boundary

## Current phase

Phase 2 in progress. Phase 1 source audit is complete.

## Completed

- Bumped the shared Python/Worker artifact contract to v14 residual-overlay semantics.
- Persisted canonical L4 evidence before Fusion materialization.
- Removed the recommendation early return that discarded valid L4 after Fusion rejection.
- Added explicit zero-adjustment fallback when Fusion is missing/rejected/incompatible.
- Preserved fail-closed behavior when canonical L4 itself is missing or invalid.
- Replaced the serving materializer's two S12-derived heads with one residual-adjustment head.
- Serving now computes `final_expected_return = base_l4_expected_return + clipped_residual_adjustment`.
- Added explicit base owner, base EV, adjustment, final EV, overlay status, and adjustment permission fields.
- Explicitly reject legacy S12 serving heads under the v14 contract.

## Verification

- Python compilation passed for the materializer, recommendation service, and evidence contracts.

## Next

- Convert artifact training/evaluation to the same v14 residual target and paired OOS L4 comparison.
- Update tests and Worker/UI evidence rendering.
