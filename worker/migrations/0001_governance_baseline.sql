-- Establishes Wrangler's ordered migration ledger after the historical
-- flat-migration era. Existing production DDL is captured by
-- schema.production.snapshot.sql; this migration intentionally changes no DDL.
SELECT 1;
