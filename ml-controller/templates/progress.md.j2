# Progress — StockVision

## Session {{ date }}

### Portfolio
- Total: ${{ total_value | int }}{% if total_return is defined %} ({{ total_return | round(2) }}%){% endif %}

- Positions: {{ positions_count }} | Cash: ${{ cash | default(0) | int }}
- MDD: {{ mdd | default(0) | round(1) }}% | Sharpe(30d): {{ sharpe | default('N/A') }}

### Today's Pipeline
- Screener: {{ screener_count }} → ML BUY: {{ ml_buy_count }} → T2: {{ t2_count }} orders
- Trades: {{ buy_count }} BUY / {{ sell_count }} SELL

### Positions
{% for p in positions %}
{{ p.symbol }} {{ p.shares }}shares cost:{{ p.avg_cost | default(p.entry_price) | round(1) }} now:{{ p.current_price | default('?') }} {{ p.unrealized_pnl_pct | default(0) | round(1) }}%
{% endfor %}
{% if not positions %}
No positions.
{% endif %}

### Model Health
- Degraded: {{ degraded_models | default('None') }}
- Optuna params version: {{ optuna_version | default('N/A') }}

### Deployments
- Worker: {{ worker_version | default('latest') }}
- ML (Modal): deployed
- Controller (Cloud Run): deployed

### Cron Schedule
```
17:30 data-update → 17:40 screener → 18:00 ml-predict → 18:05 recommendation → 18:35 obsidian
07:15 morning-setup → T2 debate → paper trading
```

### Action Items
{% for item in action_items %}
- [ ] {{ item }}
{% endfor %}
{% if not action_items %}
- [ ] Monitor pipeline execution
{% endif %}
